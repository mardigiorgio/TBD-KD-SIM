# Numerical Integrator Research

Working repository for testing and comparing numerical integration methods (explicit/implicit Euler, etc.) with CPU and GPU implementations.

## Installation

```bash
pip install -r requirements.txt
```
